export const settings = {
    // Ting eleven kan ændre på (senere i et UI)
    user: {
        theme: 'light',
        fontSize: 14,
        decimalSeparator: ',' 
    },
    // Ting som maskinrummet skal bruge
    engine: {
        precision: 10,
        timeout: 5000,
        defaultDomain: 'Reals'
    }
};